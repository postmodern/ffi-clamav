name: ffi-clamav
summary: Ruby FFI bindings for libclamav
description: Ruby FFI bindings for libclamav, part of ClamAV,
  the anti-virus toolkit for UNIX.
license: GPLv2
authors: Postmodern
email: postmodern.mod3@gmail.com
homepage: https://github.com/postmodern/ffi-clamav#readme

requirements: libclamav

dependencies:
  ffi: ~> 1.0

development_dependencies:
  rspec: ~> 2.4
  rubygems-tasks: ~> 0.2
  yard: ~> 0.8
